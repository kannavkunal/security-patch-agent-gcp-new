apiVersion: v1
kind: Service
metadata:
  name: security-patch-agent
  namespace: security-patch-agent
  labels:
    app: security-patch-agent
spec:
  type: LoadBalancer
  selector:
    app: security-patch-agent
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
