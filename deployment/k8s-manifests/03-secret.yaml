# API Keys Secret
# This file is a TEMPLATE - actual secret created by GitHub Actions workflow
# DO NOT commit real API keys here!
#
# Secret is created during deployment using:
# kubectl create secret generic security-patch-agent-api-keys \
#   --from-literal=api-keys="${{ secrets.API_KEY_PRIMARY }},${{ secrets.API_KEY_SECONDARY }}" \
#   -n security-patch-agent \
#   --dry-run=client -o yaml | kubectl apply -f -
#
apiVersion: v1
kind: Secret
metadata:
  name: security-patch-agent-api-keys
  namespace: security-patch-agent
type: Opaque
stringData:
  # API keys injected during deployment from GitHub Secrets
  # This placeholder is for reference only
  api-keys: "PLACEHOLDER_REPLACE_VIA_GITHUB_SECRETS"
